import styles from '../../styles/Episodes.module.scss'
import {motion} from 'framer-motion'

const Episodes = () => {
  return (
    <main className={styles.episodesHomeRoot} >
      <motion.section>
        hello
      </motion.section>
    </main>
  )
}

export default Episodes
